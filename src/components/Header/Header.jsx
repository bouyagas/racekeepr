import React from 'react';
import './Header.css';

const Header = props => (
  <div>
    <header>
      <h1><span>RaceKeepr</span> | plan. train. race.</h1>
    </header>
  </div>
);

export default Header;
